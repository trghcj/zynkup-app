name: zynkup
description: "Zynkup — Campus Event App | MAIT Tech Fest 2025"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Firebase (v3+ - RESOLVED CONFLICTS)
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^17.2.2

  # Google Sign-In
  google_sign_in: ^6.2.1

  # Date & Time
  intl: ^0.19.0

  # UI & Icons
  cupertino_icons: ^1.0.8
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/google_logo.png